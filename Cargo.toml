[package]
name = "mqui"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
eframe = "0.33.3"
egui = "0.33.3"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
